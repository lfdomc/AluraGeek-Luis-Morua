"use strict";
/*  const btnEliminarList = document.querySelectorAll('.btn-eliminar');
  console.log(btnEliminarList)
  btnEliminarList.forEach((btn) => {
      btn.addEventListener('click', (event) => {
          const itemId = parseInt((event.target as HTMLElement).dataset.id); // Suponiendo que cada botón tiene un data-id con el id del item
          eliminarItem(itemId); // Llama a una función para eliminar el item
      });
  });

*/
